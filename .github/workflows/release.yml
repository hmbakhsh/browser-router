name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  release:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v5
      - run: ./scripts/build-app.sh
      - run: ditto -c -k --keepParent "dist/Rootie.app" Rootie.zip
      - uses: softprops/action-gh-release@v2
        with:
          files: Rootie.zip
          generate_release_notes: true
